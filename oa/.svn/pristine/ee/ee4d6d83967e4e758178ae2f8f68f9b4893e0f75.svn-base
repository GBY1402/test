package com.lh.it.resource.common.service;

import java.util.List;

import com.lh.it.resource.common.entity.FunctionInfo;

public interface IndustryService {

	List<FunctionInfo> findByParen(Long rootNodeId);

	List<FunctionInfo> findAll();

	void update(FunctionInfo functionInfo);

	FunctionInfo findOne(Long id);

	void delete(Long id);
//删除
	void inster(FunctionInfo beas);

	List<FunctionInfo> generatorSkill(Integer level);
	
	void save(FunctionInfo functionInfo);
}
